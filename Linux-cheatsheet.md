pwd (print working direcory): shows the current directory
ls (list): list all files in the directory you are in
ls -la: list all files even hidden ones with detailes 
cd : moves to home directory if alone and to any directory if written after it
cat: reads data in file



/etc : system configuration
/home : is the home user directories
/var: variable data like logs and web files
/root: is the root directory different than / ones
